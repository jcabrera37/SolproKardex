BD: kardexcfg

TABLA: USUARIOS


TABLA: ROLES
INSERTAR ROLES
--->>> INSERT INTO `rol` (`id_rol`, `nombre_rol`) VALUES (NULL, 'ADMINISTRADOR'), (NULL, 'USUARIOS');

*/ colocar primary key
ALTER TABLE factura add PRIMARY KEY(ID_FACT);

*/colocar como autoincrementar
ALTER TABLE `cliente` CHANGE `ID_CTE` `ID_CTE` INT(11) NOT NULL AUTO_INCREMENT;

SELECT COD_PROD, NOMBRE, MARCA, SERIE, APLICACIONES FROM productos 
WHERE MARCA = 'MOPAR' AND
ESTATUS=1;